pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.40
scikit-learn>=1.3.0
xgboost>=2.0.0
ta>=0.11.0
plotly>=5.18.0
streamlit>=1.30.0
joblib>=1.3.0
pyyaml>=6.0
pytest>=7.4.0
matplotlib>=3.7.0
# Optional: backtest-driven hyperparameter search. The optimizer falls back to a
# reproducible random search if optuna is not installed.
optuna>=3.4.0
# Optional: Alpaca broker execution for forward paper/live trading. Only needed
# to run scripts/execute_alpaca.py; the rest of the tool works without it.
alpaca-py>=0.20.0
